{
  "name": "xcircl-agent",
  "version": "0.1.2",
  "private": true,
  "description": "Open-source MCP server and SDK for the xcircl regulated-care provider data API",
  "license": "MIT",
  "workspaces": [
    "packages/sdk-ts",
    "packages/mcp-server"
  ],
  "scripts": {
    "build": "npm run build -w @xcircl/sdk && npm run build -w @xcircl/mcp-server",
    "clean": "rm -rf packages/*/dist",
    "typecheck": "npm run build -w @xcircl/sdk && npm run typecheck -w @xcircl/sdk && npm run typecheck -w @xcircl/mcp-server",
    "lint": "eslint .",
    "pretest": "npm run build",
    "test": "node --import tsx --test packages/sdk-ts/test/*.test.ts packages/mcp-server/test/*.test.ts",
    "example:clinics": "node examples/01-find-clinics.mjs",
    "example:ai": "node examples/02-ai-tool-use.mjs"
  },
  "engines": {
    "node": ">=18.17"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.116.0",
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.1",
    "tsx": "^4.23.11",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.66.0"
  }
}
